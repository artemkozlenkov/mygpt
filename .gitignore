# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
env/
venv/
.venv/
.Python
pip-log.txt
pip-delete-this-directory.txt

# Docker
**/Dockerfile
# Our Caddy image build (caddy-dns/azure) — must be tracked
!caddy/Dockerfile
**/docker-compose.yml
**/.env
**/litellm_config.yaml  # Include if sensitive data is not already excluded
**/*.image.tar
**/build/

# IDEs
.vscode/
.history/
.idea/
*.iml

# Logs
*.log

# OS-specific files
.DS_Store
Thumbs.db

# GitHub secrets & local data (never commit)
*.key
*.pem
.env
.env.*
!.env.example
pgdata/
# Local DB backups (chat_user_backup_*.sql)
/chat_user_backup_*.sql
# Helm chart secret values (mirrors .env handling)
charts/mygpt/values.secrets.yaml

